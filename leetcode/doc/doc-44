dynamic programming
dp[i][j] 表示s中的前i个字符组成的字符串是否匹配了p中前j个字符组成个的字符串
dp[0][0] s和p都为空可以匹配。
1.若s为空，p[0] = '*' 或者有多个也是可以匹配 dp[0][i] = dp[0][i-1]
2.若p 为空不能匹配
3.若第j个字符串是'*' p[j - 1] = '*' 如果s中的前i+1字符的字符串已经匹配了p中j个字符的字符串则
dp[i][j] = dp[i][j-1], 此时*匹配s中(s+1, n]组成的字符串，由于*可以匹配任意的包括空字符串。
也有肯s中的前i-1个字符的字符串已经匹配了p中的前j个字符的字符串，则dp[i][j] = dp[i- 1][j]
4.若p第j个字符为普通字符或者'?' 此时如字符相等，dp[i][j] = dp[i- 1][j -1] &&(s[i-1] == p[j-1] || p[j -1] = '?')
